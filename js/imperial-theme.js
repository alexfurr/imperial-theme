jQuery(document).ready(function($) {				// Listener for modal close clicks	// When the user clicks on <span> (x), close the modal	document.getElementById('imperial-modal-close').onclick = function() {		document.getElementById('imperial-modal').style.display = "none";	}	// When the user clicks anywhere outside of the modal, close it	window.onclick = function(event) {		if (event.target == document.getElementById('imperial-modal')) {			document.getElementById('imperial-modal').style.display = "none";		}	}		//Listener for side menu clicks	$(".imperial-vert-tabs-menu .sessionNameWrap").on("click", function() {		var clickedTab = $(this).attr('id');								var sessionID = clickedTab.split('_')[1];		//console.log(sessionID);				var sessionMenuDiv = "#session"+sessionID+"_menuWrap";		$( sessionMenuDiv ).slideToggle(200);					});		//Listener for side menu clicks	$(".learning-content-menu .sessionNameWrap").on("click", function() {		var clickedTab = $(this).attr('id');						var sessionID = clickedTab.split('_')[1];		//console.log(sessionID);				var sessionMenuDiv = "#session"+sessionID+"_menuWrap";		$( sessionMenuDiv ).slideToggle(200);					});			});